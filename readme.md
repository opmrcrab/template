#Bozboz HTML Template

This is a template for kick starting a website. It (will) include(s) less in the css folder for those that do, but asl a standard css file for those that don't.

The IE conditional HTML title hack, should only be included when needed, it should not be required by default.
 